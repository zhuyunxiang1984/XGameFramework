using UnityEngine;

namespace XGameFramework.XGameApp
{
    public class XGameApp : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            var a = 0;
            
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
